# WXT build output
.output/
.wxt/

# Node modules
node_modules/

# 包管理器 lockfile（项目规范为 pnpm，只跟踪 pnpm-lock.yaml；
# npm/yarn 的 lockfile 是误用其它包管理器时产生的，忽略）
package-lock.json
yarn.lock

# 公共静态资源（由 public/ 管理）
icons/
logo.png
jsQR.min.js
totp.js

# AI 助手上下文文件
QWEN.md
CLAUDE.md
AGENTS.md

# 本地扩展密钥（用于固定开发时的扩展 ID）
extension.key
